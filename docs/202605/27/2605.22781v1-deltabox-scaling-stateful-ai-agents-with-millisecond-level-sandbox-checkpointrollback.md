---
title: "DeltaBox: Scaling Stateful AI Agents with Millisecond-Level Sandbox Checkpoint/Rollback"
title_zh: DeltaBox：通过毫秒级沙箱检查点/回滚实现有状态AI智能体的扩展
authors: "Yunpeng Dong, Jingkai He, Yuze Hou, Dong Du, Zhonghu Xu, Si Yu, Yubin Xia, Haibo Chen"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22781v1"
tags: ["query:llm-agent-memory"]
score: 6.0
evidence: 有状态代理的检查点/回滚，支持记忆状态管理
tldr: 论文针对LLM驱动的AI代理需要高频检查点/回滚（C/R）沙箱状态的场景，发现现有全量复制方法延迟高（百毫秒至秒级），提出基于变化增量复制的DeltaBox。通过DeltaFS文件系统分层冻结与DeltaCR进程增量转储及模板进程加速，实现14ms检查点和5ms回滚，显著提升代理在固定时间预算下的探索节点数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22781v1/fig-001.webp\", \"caption\": \"Figure 2. Design Overview. DeltaBox utilizes diff-based checkpoint/restore (i.e., deltaCheckpoint and deltaRestore) to enable millisecond-level checkpoint/rollback. An agent application can fully run inside a sandbox, or utilize a sandbox to execute a set of tools and use the C/R capabilities of DeltaBox to ensure prompt rollback when necessary.\", \"page\": 6, \"index\": 1, \"width\": 1046, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2605.22781v1/fig-002.webp\", \"caption\": \"Figure 9. Per-event ckpt/restore latency vs. per-event upper-layer dirt, 5 coupled-state backends (controlled microbench; 1 warmup + 3 reps per size; 80MB Python process; 2 GBVM for FC-Diff and CubeSandbox). All five backends capture both filesystem and process state for semantic parity with DeltaBox.\", \"page\": 15, \"index\": 2, \"width\": 497, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2605.22781v1/fig-003.webp\", \"caption\": \"Table 4.DeltaBox component latency breakdown, mean across SWE-bench Astropy/Django/SymPy (per-workload variance ≤4%). Fast path is the common case; slow path runs on template eviction.\", \"page\": 15, \"index\": 3, \"width\": 501, \"height\": 449}, {\"url\": \"assets/figures/arxiv/2605.22781v1/fig-004.webp\", \"caption\": \"Figure 8. RL training fan-out characterisation, 5 substrates on the same GPU cluster. (a) Substrate 1:𝑁 fan-out latency on a single GPU (144MB synthetic-anonymous parent template; 𝐾=10 prefork actions × 5MB mid-state; 5 reps). The parent here is larger than DeltaBox’s realistic ∼15MB agent.pymeasured in Table.3, so this panel stress-tests the substrate primitives rather than DeltaBox’s production parent RSS. (b) 𝑇gen (batched vLLM, 256→512 tokens, 3 reps) for three Qwen models on a single GPU, and 𝑇train for a Qwen2.5-7B LoRA-r16 fwd+bwd (bf16, grad-checkpointing, 5 reps); 𝐵∈{1, 4} on one GPU, 𝐵∈{16, 64} on 4 GPUs (FSDP, see §6.1). (c) Sync GPU util (𝑇gen+𝑇train)/(sandbox+𝑇gen+𝑇train) at 𝑁∈{16, 64} on Qwen2.5-7B, computed inline from (a) and (b).\", \"page\": 14, \"index\": 4, \"width\": 1047, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2605.22781v1/fig-005.webp\", \"caption\": \"Figure 1. Pass rate on SWE-bench Verified. (a) Linear ReAct vs. MCTS across three coding models. (b) Base vs. RL-trained across three open-weight model families.\", \"page\": 3, \"index\": 5, \"width\": 497, \"height\": 265}, {\"url\": \"assets/figures/arxiv/2605.22781v1/fig-006.webp\", \"caption\": \"Figure 5. DeltaCR architecture. Checkpointing creates both a CRIU image chain and a frozen template. Restores use the template fast path on hit, or the CRIU chain onmiss; NPD keeps external I/O off the agent path.\", \"page\": 8, \"index\": 6, \"width\": 501, \"height\": 418}, {\"url\": \"assets/figures/arxiv/2605.22781v1/fig-007.webp\", \"caption\": \"Table 1. Comparison of sandbox statemanagement approaches for AI agents.DeltaBox provides coupled checkpoint/restore of both filesystem and process state by only duplicating the changes between consecutive checkpoints for efficiency.\", \"page\": 4, \"index\": 7, \"width\": 1047, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2605.22781v1/fig-008.webp\", \"caption\": \"Figure 12. Per-event checkpoint latency on 1,689 ckpt events from 87 MCTS runs across 9 SWE-bench Verified repositories. Adaptive: pure-read cmds (LW, blue; 𝑛=1047) skip the dump; FS-mutating cmds (std, orange;𝑛=642) take the full incremental dump. Standard (gray dashed): same events forced through the std path; 62.0% of events route to the LW peak.\", \"page\": 16, \"index\": 8, \"width\": 497, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2605.22781v1/fig-009.webp\", \"caption\": \"Figure 11. Per-edit copy-up bytes (a) and physical I/O bytes (b) vs. edited-file size (log–log); real SWE-bench agent edits across three filesystem configurations, per-bin medians. Shaded band marks the typical agent edit range. ext4 and XFS-withoutreflink coincide on (a): copy-up benefit comes entirely from reflink, not XFS.\", \"page\": 16, \"index\": 9, \"width\": 461, \"height\": 428}]"
motivation: 现有沙箱检查点/回滚全量复制导致高延迟，限制AI代理深度搜索和大规模扇出。
method: 提出DeltaState抽象，通过DeltaFS分层文件系统和DeltaCR进程增量转储与模板进程fork加速，实现基于变化的C/R。
result: DeltaBox实现毫秒级C/R（检查点14ms，回滚5ms），在SWE-bench和RL微基准中代理探索节点数显著增加。
conclusion: 基于变化复制的沙箱C/R可有效扩展AI代理的状态探索能力，具有显著的性能提升。
---

## 摘要
基于LLM的AI智能体需要高频状态探索（例如测试时树搜索和强化学习），依赖于完整沙箱状态的快速检查点和回滚（C/R），包括文件及进程状态（如内存、上下文等）。现有机制会复制整个状态，每次C/R造成数百毫秒到秒级的延迟，严重阻碍深度搜索和大规模扇出。本文观察到AI智能体中后续检查点具有高度相似性。因此，沙箱不应完全复制，而应仅复制连续检查点之间的变化（关键洞察）。然而，实现这一想法并非易事，主要由于缺乏操作系统支持。本文提出一种新的操作系统级抽象DeltaState，通过两种协同设计的操作系统机制为AI智能体实现基于变化的事务性C/R。首先，DeltaFS通过将文件状态组织成层，并在检查点期间动态冻结可写层并插入新层，将文件更新简化为写时复制，使回滚成为简单的层切换，从而实现基于变化的文件系统C/R。其次，DeltaCR使用增量转储实现基于变化的进程状态C/R，并通过绕过传统管道直接从冻结模板进程fork()来加速回滚。我们随后提出DeltaBox，一种通过这两种新机制实现毫秒级C/R的新型智能体沙箱。在SWE-bench和RL微基准测试上的评估表明，DeltaBox能在毫秒级延迟内完成检查点和回滚（分别为14ms和5ms），使智能体在固定时间预算下能够探索显著更多的节点。

## Abstract
LLM-powered AI agents require high-frequency state exploration (e.g., test-time tree search and reinforcement learning), relying on rapid checkpoint and rollback (C/R) of the complete sandbox state, including files and process state (e.g., memory, contexts, etc.). Existing mechanisms duplicate the entire state, causing hundreds of milliseconds to seconds of latency per C/R, which severely bottlenecks deep search and large-scale fan-outs.   This paper observes that subsequent checkpoints in AI agents are highly similar. Therefore, instead of full duplication, a sandbox should only duplicate the changes between consecutive checkpoints (Key Insight). However, it is non-trivial to realize the idea, mainly due to the missing OS supports. This paper proposes a new OS-level abstraction, DeltaState, to enable the change-based transactional C/R for AI agents with two co-designed OS mechanisms. First, DeltaFS enables change-based filesystem C/R by organizing the file states into layers and dynamically freezing the writable layer and inserting a new one during checkpoint, reducing file updates to copy-on-write, and making rollback a simple layer switch. Second, DeltaCR enables change-based process state C/R using incremental dumps, and accelerates rollback by bypassing traditional pipelines to directly fork() from a frozen template process. We then present DeltaBox, a novel agent sandbox achieving millisecond level C/R through the two new mechanisms. Evaluations on SWE-bench and RL micro-benchmarks show DeltaBox completes checkpoint and rollback in millisecond-level latency (14ms and 5ms, respectively), empowering agents to explore substantially more nodes under fixed time budgets.