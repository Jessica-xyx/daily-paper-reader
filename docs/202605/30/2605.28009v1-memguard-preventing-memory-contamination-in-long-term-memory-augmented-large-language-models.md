---
title: "MemGuard: Preventing Memory Contamination in Long-Term Memory-Augmented Large Language Models"
title_zh: MemGuard：防止长期记忆增强的大型语言模型中的记忆污染
authors: "Hyeonjeong Ha, Jeonghwan Kim, Cheng Qian, Jiayu Liu, William M. Campbell, Yue Wu, Yuji Zhang, Kathleen McKeown, Dilek Hakkani-Tur, Heng Ji"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28009v1"
tags: ["query:llm-agent-memory"]
score: 9.0
evidence: 类型感知的记忆框架防止内存污染
tldr: "长时记忆增强的大语言模型存在异质记忆污染问题，不同功能类型的记忆被混用导致生成错误。本文提出MemGuard框架，在写入时分配功能角色，隔离存储并选择性组合证据，从而减少污染。在幻觉和长程对话基准上，内存可靠性提升28.27%，检索token减少5.8倍。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28009v1/fig-001.webp\", \"caption\": \"Figure 1: Heterogeneous memory contamination. Weak functional boundaries cause heterogeneous memories, including semantic constraints, episodic observations, and procedural guidance, to be stored, retrieved, and composed as interchangeable evidence. This contamination propagates across the memory writing and retrieval, leading to persistent hallucinations and degraded reasoning quality.\", \"page\": 2, \"index\": 1, \"width\": 842, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.28009v1/fig-002.webp\", \"caption\": \"Table 1: Hallucination evaluation on HaluMem. Hallucination is evaluated at three stages: memory extraction, update, and answer generation. R/P denote recall/precision; C/H/O denote Correct/Hallucination/Omission rates; and Acc. denotes anti-hallucination accuracy. Higher is better for R, P, Acc., F1, and C; lower is better for H and O. † indicates results taken from the original paper.\", \"page\": 7, \"index\": 2, \"width\": 846, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2605.28009v1/fig-003.webp\", \"caption\": \"Table 2: Utility evaluation on general long-horizon conversation benchmarks. # Avg. Token refers to the average number of tokens in retrieved memories. Accuracy (%) is measured through LLM-as-a-Judge. †, ∗ indicates results taken from the original paper, while “–” indicates results not reported in the original paper.\", \"page\": 7, \"index\": 3, \"width\": 848, \"height\": 349}, {\"url\": \"assets/figures/arxiv/2605.28009v1/fig-004.webp\", \"caption\": \"Table 3: Ablations of different components.\", \"page\": 8, \"index\": 4, \"width\": 461, \"height\": 776}, {\"url\": \"assets/figures/arxiv/2605.28009v1/fig-005.webp\", \"caption\": \"Figure 3: Overview of MEMGUARD. At write time, MEMGUARD reorganizes a conversation into atomic knowledge units, constructs directed relations among them, verifies missing information, and writes each atom to a type-isolated memory store. At retrieval time, the model routes queries adaptively to relevant memory types and selectively composes retrieved atoms via a relational knowledge graph, reducing cross-type interference. By preserving functional boundaries, MEMGUARD prevents heterogeneous memory contamination.\", \"page\": 5, \"index\": 5, \"width\": 937, \"height\": 319}]"
motivation: 现有长时记忆系统将用户事实、事件和规则等不同功能记忆混在一起，导致上下文特定事件被泛化或功能不兼容的记忆误导模型生成。
method: 提出MemGuard，为每条记忆赋予显式功能角色，隔离不同类型记忆并维护关系，在检索时仅从必要记忆类型中选择性组合证据。
result: "在幻觉和长程对话基准上，与先前方法相比，记忆可靠性最高提升28.27%，同时检索内存token减少最多5.8倍。"
conclusion: 长时推理的可靠性依赖于对异质记忆进行有原则的组织和选择性使用。
---

## 摘要
记忆增强的大型语言模型通过跨交互维持长期记忆，将推理扩展到固定上下文窗口之外。然而，现有的记忆系统经常将稳定的用户事实、情景事件和行为规则折叠到一个共享空间中，使得功能不同的记忆可以被检索并作为可互换的证据使用。我们识别出这种失败模式为异质性记忆污染，其中上下文特定的事件被过度泛化，或者语义相关但功能不兼容的记忆误导生成。为此，我们引入了MemGuard，一种类型感知的记忆框架，在记忆构建和检索过程中保留功能记忆边界。它在写入时为每个记忆分配显式的功能角色，维护跨类型隔离记忆的关系，并仅从必要的记忆类型中选择性组合证据，减少来自不相关或功能不兼容证据的污染。在幻觉和长程对话基准测试中，MemGuard将记忆可靠性提高了最多28.27%，同时检索的记忆令牌比先前方法减少最多5.8倍。这些结果表明，可靠的长期推理取决于对异质性记忆的有原则组织和选择性使用。

## Abstract
Memory-augmented large language models extend reasoning beyond a fixed context window by maintaining long-term memory across interactions. However, existing memory systems often collapse stable user facts, episodic events, and behavioral rules into a shared space, allowing functionally distinct memories to be retrieved and used as interchangeable evidence. We identify this failure mode as heterogeneous memory contamination, where context-specific events become overgeneralized claims, or semantically relevant but functionally incompatible memories mislead generation. To this end, we introduce MemGuard, a type-aware memory framework that preserves functional memory boundaries during memory construction and retrieval. It assigns each memory an explicit functional role at write time, maintains relations across type-isolated memories, and selectively composes evidence only from necessary memory types, reducing contamination from irrelevant or functionally incompatible evidence. Across hallucination and long-horizon conversation benchmarks, MemGuard improves memory reliability by up to 28.27% while retrieving up to 5.8x fewer memory tokens than prior methods. These results suggest that reliable long-term reasoning depends on principled organization and selective use of heterogeneous memory.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

现有长时记忆增强的大语言模型将用户事实（语义记忆）、情景事件（情景记忆）和行为规则（程序记忆）等不同类型的记忆混杂存储在共享空间中。由于缺乏功能边界，模型在写入和检索时会将这些异构记忆视为可互换的证据，导致**异质性记忆污染**（Heterogeneous Memory Contamination）：例如，一次性的情景观察被过度泛化为稳定事实，或语义相关但功能不兼容的记忆误导生成，最终引发持久性幻觉。论文通过实验发现，97.7%的不可验证性错误源于写入时污染，63.8%的事实性错误源于检索时污染，证实了该问题。为此，论文提出**MemGuard**，通过在记忆的写入和检索阶段显式保留功能类型边界，从根本上减少污染。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：将记忆按功能类型（语义/情景/程序）隔离存储，并在检索时根据查询动态选择相关类型，通过关系知识图进行受控的证据组合。

**关键技术细节**：
- **写入时记忆重组**：
  1. **类型感知知识分解**：LLM将对话分解为原子记忆单元，每个单元赋予单一类型（`semantic`/`episodic`/`procedural`），限制异构知识合并。
  2. **自验证提取**：初次分解后，模型检查对话中是否有遗漏信息，补充缺失原子。
  3. **关系知识图构建**：在原子间建立有向边（如`supports`/`contradicts`/`derived_from`等），保留跨类型的依赖关系，但原子内容仍按类型隔离。
  4. **类型隔离写入**：每个原子路由到对应类型的存储区，只与同类型已有记忆比较并执行`ADD`/`UPDATE`/`SKIP`操作，避免不同功能记忆相互覆盖。

- **检索时动态路由**：
  1. **查询自适应类型路由**：使用LLM软路由输出三种类型的置信度权重，按比例分配检索预算`K`，确保只从相关类型存储中检索。
  2. **关系知识组合**：对每个初步检索结果，在关系图上进行广度优先搜索（最多`h_max`跳），扩充相关节点，并用跳数衰减（λ=0.85）重排序，最终选择Top-K组成上下文。

该流程用文字描述：先分解对话→自验证→建图→类型隔离存储；检索时先路由分配预算→语义相似检索→图扩展→重排序→生成答案。

### 3. 实验设计：使用的数据集、基准和对比方法

- **数据集**：
  - **HaluMem**：幻觉诊断基准，专门测试记忆写入、更新、回答时的抗幻觉能力（使用Medium子集）。
  - **LoCoMo**：长程多人对话基准（10个对话，平均26k tokens，约200问/对话）。
  - **LongMemEval**：400个问题的用户-助手对话记忆测试。
  - **PerLTQA**：141个角色，8,593个问题，关注个性化记忆。

- **基准方法**：覆盖四类范式：
  - 平面语义：Mem0、Memobase
  - 结构化/图：Mem0-Graph、Zep、Supermemory
  - 认知/层次：MIRIX、MemOS
  - 智能体动态：A-Mem

- **评估指标**：
  - HaluMem：记忆提取（Recall、Precision、F1、Anti-hallucination Accuracy）、更新（Correct/Hallucination/Omission率）、问答（Correct/Hallucination/Omission率）。
  - 其他基准：LLM-as-Judge的答案准确率。

### 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量或训练时长。实验基于LLM调用（GPT-4.1-mini作为基础模型，GPT-4.1或GPT-4o-mini作为判断模型），同时使用`text-embedding-3-small`作为嵌入模型。因此算力消耗主要体现在API调用成本上，未报告具体硬件配置。

### 5. 实验数量与充分性

实验较为充分：
- 在**4个数据集**上评估（HaluMem、LoCoMo、LongMemEval、PerLTQA）。
- 涵盖**三种记忆生命周期任务**：提取、更新、问答。
- 对比**9种基线方法**，包含不同范式。
- 进行了**消融实验**（表3）：去除关系组合和路由两个组件。
- 分析了**不同检索预算K和关系跳数h**的影响（图5）。
- 进行了**错误阶段分析**（图2）和**特征空间可视化**（LDA，图4a）。

实验公平性：HaluMem基线使用更强的GPT-4o，而MemGuard使用较弱的GPT-4.1-mini，比较保守但仍取得领先；其他基准使用相同模型设定，并复现了A-Mem。

### 6. 论文的主要结论与发现

1. **异质性记忆污染是长时记忆LLM中持久幻觉的关键来源**，写入和检索阶段都会引入污染。
2. **MemGuard通过类型隔离存储和动态路由，显著降低幻觉**：在HaluMem上抗幻觉准确率89.53%（+28.27%），记忆更新正确率70.79%（+9.38%）。
3. **在保持高准确率的同时，检索的Token数大幅减少**：比A-Mem减少约4.5倍，比Mem0减少约5.8倍。
4. **关系知识组合比单纯增加检索量更有效地提升证据覆盖**（图5），同时保持好的可答性校准（尤其在对抗性查询上提升超20%）。
5. 类型隔离和路由两者互补（消融实验验证），共同保障记忆可靠性。

### 7. 优点：方法或实验设计上的亮点

- **问题定义清晰**：首次系统定义“异质性记忆污染”这一失败模式，并通过定量错误分析（图2）提供了实证支撑。
- **方法创新性强**：将认知科学中功能分离的记忆系统理论引入LLM记忆管理，提出写入时类型感知分解 + 检索时自适应路由 + 关系图组合的完整框架。
- **实验设计全面**：同时评估提取、更新、生成三个阶段，覆盖不同类型基准和多种基线，并且进行了深层分析（LDA、budget消融）。
- **鲁棒性验证充分**：通过对抗性查询测试了模型在不充分证据下正确“回避”的能力，结果表明MemGuard显著优于基线。
- **效率与效果的平衡**：在降低检索成本的同时提升可靠性，具有实用价值。

### 8. 不足与局限

- **未控制生成时行为**：即使检索内容正确，基础LLM仍可能因语义误解或推理错误导致组合失败（composition failure）。论文承认这一点。
- **计算成本**：作为基于LLM的智能体框架，每次写入和检索都需多次调用LLM（分解、验证、路由、更新决策），可能比端到端训练方法昂贵。
- **评估范围有限**：仅聚焦于长程对话和幻觉基准，未在具身智能、长期决策任务等更广泛场景中验证。
- **依赖单一嵌入模型**：检索使用`text-embedding-3-small`，未分析不同编码器的影响。
- **资源开销未报告**：缺少具体的GPU/时间等资源消耗数据，难以直接衡量部署成本。

（完）
