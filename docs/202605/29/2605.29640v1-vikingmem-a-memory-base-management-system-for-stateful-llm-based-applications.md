---
title: "VikingMem: A Memory Base Management System for Stateful LLM-based Applications"
title_zh: VikingMem：基于状态的大语言模型应用的记忆基础管理系统
authors: "Jiajie Fu, Junwen Chen, Mengzhao Wang, Aoxiang He, Maojia Sheng, Xiangyu Ke, Yifan Zhu, Yunjun Gao"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.29640v1"
tags: ["query:llm-agent-memory"]
score: 8.0
evidence: 用于有状态LLM应用的记忆库管理系统
tldr: "针对LLM有限上下文窗口导致的状态管理难题，提出Memory Base新范式，通过选择性提取、状态演化与通用抽象管理长期交互记忆。实现VikingMem系统，基于VikingDB的事件/实体抽象、时间压缩与加权召回，在长期记忆基准上检索效果提升30%且保持低延迟。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.29640v1/fig-001.webp\", \"caption\": \"Figure 3: The extract paradigm in VikingMem\", \"page\": 6, \"index\": 1, \"width\": 458, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2605.29640v1/fig-002.webp\", \"caption\": \"Figure 5: Use case for Agent Memory\", \"page\": 8, \"index\": 2, \"width\": 433, \"height\": 343}, {\"url\": \"assets/figures/arxiv/2605.29640v1/fig-003.webp\", \"caption\": \"Figure 1: The definition schema of event, entity for memory extraction, and the built-in operators in VikingMem\", \"page\": 4, \"index\": 3, \"width\": 474, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2605.29640v1/fig-004.webp\", \"caption\": \"Table 1: LLM-as-a-Judge evaluation scores (%, with higher values denoting superior performance) and Search Latency (second) for each question category in the LOCOMO and LongMemEval dataset. Scores for baselines annotated with a superscript † are sourced from published evaluation results, while the search latencies for Memobase in LOCOMO and Zep in LongMemEval were measured from our own experiments. Best results are in bold; second-best results are with underline. In LongMemEval, \\\"SSU\\\" stands for \\\"single-session-user\\\", \\\"MS\\\" denotes \\\"multi-session\\\", \\\"SSP\\\" represents \\\"single-session-preference\\\", \\\"TR\\\" indicates \\\"temporal-reasoning\\\", \\\"KU\\\" refers to \\\"knowledge-update\\\", and \\\"SSA\\\" means \\\"single-session-assistant\\\".\", \"page\": 9, \"index\": 4, \"width\": 960, \"height\": 812}, {\"url\": \"assets/figures/arxiv/2605.29640v1/fig-005.webp\", \"caption\": \"Figure 2: The pipeline of VikingMem\", \"page\": 5, \"index\": 5, \"width\": 911, \"height\": 475}]"
motivation: 现有记忆方法依赖简单提取或专用提示，缺乏通用性且在多样化下游任务中表现不佳。
method: 提出Memory Base范式，以选择性提取、状态演化与通用抽象为核心；构建VikingMem系统，采用事件-实体双抽象、事件中心提取、时间线压缩与时间加权召回。
result: "在长期记忆基准上，VikingMem相较基线在记忆检索有效性上提升高达30%，并维持低延迟。"
conclusion: VikingMem通过新颖的Memory Base管理范式，有效解决了LLM长期交互的状态管理问题，显著提升记忆检索性能且具通用性。
---

## 摘要
大语言模型已经彻底改变了交互式应用；然而，它们有限的上下文窗口对于维护有状态的长期交互构成了关键的数据管理挑战。现有的记忆方法通常依赖于过于简单的提取方法，导致记忆不完整，或者使用针对单一用例（如聊天机器人）定制的、僵化的单用途记忆提取提示。因此，它们缺乏通用性，并且在多样化的下游任务中表现不佳。为了弥补这一差距，我们引入了记忆基础（Memory Base），一种用于管理长期交互持久状态的新型数据管理范式。它的特点是三个核心原则：从原始信息流中选择性提取高价值记忆；固有的状态性和演化，其中记忆内容逐步总结、纠正并随时间加权以优先处理最近交互；以及一种通用的抽象范式，旨在跨不同应用（包括教育、推荐和代理记忆）实现稳健的可迁移性。在此基础上，我们提出了VikingMem，一个在VikingDB向量引擎上实现的端到端记忆基础管理系统。VikingMem通过互连的事件和实体抽象实现了这一范式。它具有以事件为中心的记忆提取功能，以选择性处理复杂的信息流，同时实体由事件动态更新以实现有状态的演化。通过主题时间线进行时间压缩以及时间加权召回，系统逐步生成高级摘要记忆，优先处理最近项目，并压缩和淡出较旧项目。在长期记忆基准测试上的广泛评估表明，VikingMem在记忆检索有效性方面比基线高出多达30%，同时保持了交互应用所需的低延迟。

## Abstract
Large Language Models have revolutionized interactive applications; however, their finite context windows pose a critical data management challenge for maintaining stateful, long-term interactions. Existing memory approaches often rely on simplistic extraction methods that lead to incomplete memories or use rigid, single-purpose memory extraction prompts tailored to a single use case, such as chatbots. Consequently, they lack generalizability and perform poorly across diverse downstream tasks. To bridge this gap, we introduce the Memory Base, a novel data management paradigm for managing the persistent state of long-term interactions. It is characterized by three core principles: selective extraction of high-value memories from raw information streams; inherent statefulness and evolution, where memory content is progressively summarized, corrected, and temporally weighted to prioritize recent interactions; and a generalizable abstraction paradigm designed for robust transferability across diverse applications, including education, recommendation, and agent memory. Building on this foundation, we present VikingMem, an end-to-end Memory Base Management System implemented on the VikingDB vector engine. VikingMem materializes this paradigm through interconnected event and entity abstractions. It features event-centric memory extraction to selectively handle complex information streams, while entities are dynamically updated by events to achieve stateful evolution. Using temporal compression via a topic-wise timeline and time-weighted recall, the system progressively produces high-level summary memories, prioritizes recent items, and compresses and fades older ones. Extensive evaluations on long-term memory benchmarks demonstrate that VikingMem outperformes baselines by up to 30% in memory retrieval effectiveness while maintaining the low latency essential for interactive applications.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机与背景）
- **问题**：大语言模型（LLM）尽管能力强大，但其有限的上下文窗口（context window）使得维持长期间、有状态的交互变得困难。现有的记忆方法要么过于简单（导致记忆不完整），要么僵化地为单一用例（如聊天机器人）定制提示，缺乏通用性，在多样化下游任务中表现不佳。
- **背景**：现代LLM应用（如个性化助手、自主智能体、教育辅导）需要跨多轮对话、跨会话的持久状态。但当前生产环境通常将长期状态分散在日志、浅缓存和任务特定启发式中，导致连续性断裂、矛盾记忆和高开发成本。
- **整体含义**：需要一种专门的、服务级的“记忆基础”（Memory Base）管理范式，能够选择性提取高价值信息、保持状态演化并支持多种业务场景。论文提出的VikingMem正是实现这一范式的端到端系统。

### 2. 方法论：核心思想、关键技术细节

#### 2.1 核心思想
- **Memory Base 三大原则**：
  - **选择性信息提取**：从低信噪比的原始数据流中提取高价值的离散事件，减少噪声和存储。
  - **固有的状态性与演化**：记忆不是静态档案，而是动态生命；通过事件驱动实体持续更新，并随时间压缩和遗忘。
  - **通用化抽象范式**：使用可配置的“事件-实体”（Event-Entity）模型，支撑不同应用场景（社交、教育、代理、推荐等），无需为每个场景重写系统。

#### 2.2 关键技术细节
- **Event-Entity 抽象**：
  - *事件（Event）*：离散、带时间戳的、可提取的回忆片段，是记忆的原子单位。
  - *实体（Entity）*：持久演化的状态表示（如用户画像、工具使用经验），由事件通过聚合表达式和操作符（Operator）增量更新。
  - 关系：`entity = SELECT OP(event.content) FROM Events WHERE filters GROUP BY keys`，其中`OP`来自内置操作符库（如`LLM_MERGE`、`SUM`、`TIME_COMPRESS`）。
- **记忆提取**：
  - **单次提取（One-pass Extraction）**：将多个记忆类型的模式（Schema）编译成单一提示，LLM一次性从原始输入中提取所有事件和实体更新补丁，大幅减少token消耗。
  - **智能记忆分割（Intelligent Memory Segmentation）**：两阶段过程——①语义显著性过滤（剔除问候等无用内容）；②事件中心化划分（确定连续主题的起止坐标，合并非连续但语义相关的段落）。
  - **实体无LLM更新算法（EUA）**：对字符串类型实体字段，提取器输出SEARCH/REPLACE格式补丁；算法通过编辑距离近似匹配找到旧实体中的对应位置并替换，无需额外LLM调用，降低延迟。
- **记忆管理**：
  - 去重、操作符（统计类+LLM类）、**时间线压缩**（将相关事件按主题分组，旧事件到期后合成为高层摘要，释放底层事件）。
  - 构建**关键词图**：关键词嵌入通过包含该词的所有记忆片段嵌入平均得到，用于辅助召回。
- **记忆检索**：
  - **多路径召回**：主路径（混合向量+稀疏检索）与辅助路径（关键词图），结合时间衰减权重与业务重要性权重。
  - **多向量重排**：借鉴ColBERT的后期交互机制，预计算并压缩记忆的ColBERT向量，实现高效重排。

### 3. 实验设计

#### 3.1 数据集与场景
- **LOCOMO**：10个长对话，每对话平均1000+消息，涵盖单跳、多跳、时间推理、开放域问题。
- **LongMemEval（子集LongMemEval_s）**：500个长对话，平均约115k token，含多会话推理、知识更新等类别。

#### 3.2 Benchmark 与对比方法
- **对比方法**（共8个）：Mem0、Mem0-graph、Zep、RAG、Full-Context（全历史注入）、Claude Native Memory、OpenClaw、Mirix。
- **评价指标**：LLM Judge Score（%）、F1-Score、搜索延迟（p50/p95 秒数）。
- **LLM设置**：LOCOMO用GPT-4.1-mini和GPT-4o-mini；LongMemEval用GPT-4o-mini和GPT-4o。

#### 3.3 实验内容
- 端到端效果与效率对比（表1）
- 单次提取与EUA消融（表2）
- 存储效率对比（表3）
- 系统组件消融（表4）
- 操作符使用频率分析（表5，来自100+客户应用场景）
- F1-score补充评估（表6）

### 4. 资源与算力
- **文中明确说明的算力**：
  - 向量数据库服务：单节点32 vCPU（Intel Xeon Platinum 8582C）、8GB内存。
  - 嵌入服务：单张NVIDIA A30 GPU（24GB显存）、16 CPU、64GB内存。
- **未说明**：LLM模型调用（GPT-4.1-mini等）是通过API，不涉及本地训练显存；未报告训练时长或微调资源，因为VikingMem本身不训练模型。

### 5. 实验数量与充分性
- **实验组数**：至少包含6组主要实验（端到端效果×2数据集、提取效率、存储效率、消融、操作符频率、F1），其中端到端效果涵盖两个LLM设置，表1含多个子类别。
- **充分性**：实验覆盖了效果、效率、存储、消融等多个维度，对比了所有代表性基线（8个），使用两个公认的长期记忆基准。消融研究（表4）验证了各模块贡献。F1-score补充避免了仅依赖LLM Judge的偏差。
- **公平性**：与基线用相同prompt设置（引用自Zep等先前工作），基线结果多来自原始论文或自行复现；多次运行取平均以减少随机性。实验设计客观、公平。

### 6. 主要结论与发现
- **VikingMem在LLM Judge Score上达到SOTA**：在LOCOMO上以GPT-4o-mini取得88.83%（第二成绩为Mirix 78.66%），在LongMemEval上以GPT-4o取得75.80%（第二为Zep 70.40%）。
- **延迟极低**：p50延迟约0.2-0.25秒，p95延迟低于1秒，远超同效果基线（Mirix p95达25秒）。
- **存储效率高**：相比Naïve RAG，存储压缩率达83.2%，同时效果不降反升。
- **单次提取和EUA算法显著降低token成本与延迟**：从$0.35降至$0.07，时间从11.02s降至7.42s。
- **各组件均有贡献**：智能分割贡献最大（提升5.32分），重排和关键词图也重要。
- **操作符分布体现场景差异**：教育、代理场景多用统计操作符；社交陪伴多用LLM_MERGE和时间压缩。

### 7. 优点
- **系统设计创新**：首次形式化定义“Memory Base”范式，以事件-实体抽象为核心，将记忆管理提升为可复用代数（而非提示工程）。
- **高效性**：单次提取、EUA、多向量重排等使得系统在保持SOTA效果的同时，计算和延迟代价极低，适合生产环境。
- **通用性**：抽象模型可覆盖社交、教育、代理、搜索推荐等多种场景，无需修改核心引擎。
- **工程实绩**：已在字节跳动大规模部署（单一租户日处理10亿+token），并开源核心能力（OpenViking）。
- **实验充分**：不仅报告LLM Judge，还补充F1-score；消融实验系统；操作符频率分析来自真实客户数据，有说服力。

### 8. 不足与局限
- **实验局限**：仅在两个公开对话记忆基准上评估，缺乏对更多样化任务（如长文档问答、多轮结构化任务）的测试；数据集为英文，未涵盖多语言场景。
- **依赖LLM质量**：提取和LLM_MERGE等操作依赖LLM能力，若LLM可靠性不足可能影响记忆质量；未讨论对开源/小型LLM的兼容性。
- **关键词图开销**：消融显示关键词图增加约106ms延迟，对于极低延迟场景可能构成挑战。
- **安全与隐私**：文中提及隐私政策和审计（§1），但未深入讨论记忆持久化带来的隐私风险、记忆删除或修正机制。
- **可扩展性推测**：文中提到单租户日处理10亿+token，但未提供集群扩展的具体压力测试结果。
- **与未来更强大LLM的关系**：作者承认长上下文窗口LLM可能减少记忆系统的需求，但仍强调成本和效率问题；但未量化对比不同长上下文模型下记忆系统的增益衰减情况。

（完）
