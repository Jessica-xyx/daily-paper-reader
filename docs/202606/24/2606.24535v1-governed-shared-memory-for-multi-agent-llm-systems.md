---
title: Governed Shared Memory for Multi-Agent LLM Systems
title_zh: 多智能体LLM系统的受控共享内存
authors: "Yanki Margalit, Nurit Cohen-Inger, Erni Avram, Ran Taig, Oded Margalit"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24535v1"
tags: ["query:llm-agent-memory"]
score: 9.0
evidence: 形式化定义多智能体系统共享记忆问题，识别失效模式并提出可治理原语
tldr: "多智能体LLM系统的共享内存面临未经授权泄漏、过时传播、矛盾持续和溯源崩溃四类失效模式。MemClaw服务通过限定范围检索、时间取代、溯源追踪和策略管控等原语解决这些问题。在ArgusFleet测试中，溯源实现100%深度4推导链重建，传播零跨舰队泄漏。生产环境暴露了非对称作用域强制和流水线排序冲突等架构缺陷，表明长上下文检索不足，需显式系统级抽象。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24535v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1400, \"height\": 1030, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 862, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 859, \"height\": 566, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 856, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 860, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 848, \"height\": 560, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24535v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 853, \"height\": 566, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24535v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 839, \"height\": 280, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24535v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 896, \"height\": 421, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24535v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1843, \"height\": 1299, \"label\": \"Table\"}]"
motivation: 多智能体LLM环境需要鲁棒的共享知识管理，现有长上下文检索无法解决生产场景中的内存失效问题。
method: 形式化fleet-memory问题，定义scoped retrieval、temporal supersession、provenance tracking和policy-governed propagation原语，并实现为MemClaw服务。
result: "溯源100%重建深度4推导链，传播零泄漏；发现子租户作用域绕过和近重复门与矛盾检测器冲突的架构问题。"
conclusion: 生产多智能体内存需要显式系统级抽象，实时评估对暴露设计遗漏问题至关重要。
---

## 摘要
多智能体LLM环境需要健壮的共享知识管理机制。本文形式化了群组记忆问题，并识别了四种基础故障模式：未授权泄漏、过时传播、矛盾持续和来源崩溃。为解决这些问题，我们定义了明确的系统级原语：作用域检索、时间接替、来源追踪和策略驱动的记忆传播。这些原语在MemClaw（一个生产级多租户记忆服务）中实现，并通过ArgusFleet（一个可重现的测试框架）评估了四个治理维度。与基线对比不同，本研究测量了实际生产服务，强调真实世界的架构见解和负面结果。关键评估结果：来源——成功重建了100%的四层推导链，每跳延迟低于一秒，且作者身份正确。传播——展示了高群组内可见性，且无跨群组泄漏。在强写模式下，写入到可见的延迟优化为单次搜索往返。发现的生产架构问题：不对称作用域执行——租户隔离有效，但子租户作用域在直接通过ID获取代理作用域凭据时最初被绕过（研究期间已披露并修复）。管道排序冲突——虽然矛盾接替对已接受的写入有效，但同步的近重复门控可能过早拒绝矛盾的写入，而异步矛盾检测器尚未评估它们。结论：仅靠长上下文检索不足以支撑生产级多智能体记忆。受控共享内存需要明确的系统级抽象，实时评估对于暴露仅靠设计处理无法发现的执行和管道排序故障至关重要。

## Abstract
Multi-agent LLM environments require robust mechanisms for shared knowledge management. This paper formalizes the fleet-memory problem and identifies four foundational failure modes: unauthorized leakage, stale propagation, contradiction persistence, and provenance collapse. To address these, we define explicit systems-level primitives: scoped retrieval, temporal supersession, provenance tracking, and policy-governed memory propagation. These primitives are implemented in MemClaw, a production multi-tenant memory service, and evaluated via ArgusFleet, a reproducible harness testing four governance dimensions. Rather than a baseline comparison, this study measures a live production service, emphasizing real-world architectural insights and negative results. Key Evaluation Results Provenance: Successfully reconstructed 100% of depth-four derivation chains with correct writer identity at sub-second per-hop latency. Propagation: Demonstrated high intra-fleet visibility with zero cross-fleet leakage. Under strong write mode, write-to-visible latency was optimized to a single search round-trip. Production Architectural Issues Discovered Asymmetric Scope Enforcement: Tenant isolation held, but sub-tenant scope was initially bypassed on direct GET-by-id requests for agent-scoped credentials (disclosed and remediated during the study). Pipeline Ordering Conflict: While contradiction supersession works for admitted writes, a synchronous near-duplicate gate can prematurely reject contradictory writes before the asynchronous contradiction detector can evaluate them. Conclusion: Long-context retrieval alone is insufficient for production multi-agent memory. Governed shared memory demands explicit systems-level abstractions, and live evaluation is vital to expose enforcement and pipeline-ordering failures missed by design-only treatments.

---

## 论文详细总结（自动生成）

# Governed Shared Memory for Multi-Agent LLM Systems 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有LLM记忆系统大多为单智能体设计（一个用户、一次对话、一个检索流），当多个智能体（agent fleet）共享持久化记忆时，会出现单智能体场景不存在的正确性和治理需求：谁可以检索哪段记忆？两个智能体写入矛盾事实时如何解决？知识如何安全地跨越智能体边界传播？每条检索到的记忆能否追溯到其原始写入者？当底层状态变化时，过时的记忆如何被无效化？
- **研究动机**：随着企业copilot、工作流编排器、客服系统、研究智能体等向多智能体群组演化，记忆不再是“聊天历史”，而是共享的操作状态。作者主张AI记忆正在从“上下文窗口问题”演变为“分布式系统问题”，需要治理、同步、时序正确性、溯源和访问控制等系统级原语。
- **整体含义**：该论文将多智能体记忆系统形式化为一个分布式治理问题，并提出了一个包含作用域、溯源、时间接替等原语的架构，以及一个生产级实现MemClaw和一个可重现评估框架ArgusFleet。通过实时测量揭示了仅靠设计难以暴露的架构缺陷。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将群组记忆系统形式化为五元组 \(F = (A, M, G, P, T)\)，其中 \(A\) 是智能体集合，\(M\) 是共享记忆基质，\(G\) 是治理与策略层，\(P\) 是溯源元数据，\(T\) 是时序排序与接替语义。记忆写入是一个状态转换：\(w_i = (a_i, c_i, s_i, t_i, p_i)\)，其中 \(a_i\) 是写入智能体，\(c_i\) 是内容，\(s_i\) 是可见性作用域，\(t_i\) 是时序顺序，\(p_i\) 是溯源元数据。检索定义为 \(r_j = f(q_j, a_j, g_j, t_j)\)，其中 \(q_j\) 是查询，\(a_j\) 是请求智能体，\(g_j\) 是治理约束，\(t_j\) 是时序正确性条件。
- **关键技术细节**：
  - **作用域（Scope）**：定义了四级作用域：Agent-local（仅一个智能体可见）、Team-shared（一组智能体共享）、Tenant-global（整个租户环境）、Restricted（显式策略约束）。每个检索操作必须同时满足语义相似度和作用域的治理策略。
  - **时间接替（Temporal Supersession）**：记忆被视为演进状态而非不可变的追加日志。每个记忆对象携带创建时间戳、可选的接替引用、矛盾标记、溯源元数据和置信状态。矛盾写入通过时序排序和策略感知的解决规则来处理。
  - **溯源图（Provenance Graph）**：每个记忆对象存储写入者身份、源系统、派生历史和修改谱系。支持可审计性、调试、可追溯性和合规验证。
  - **策略管控检索（Policy-Governed Retrieval）**：检索过程包括：①语义候选生成 → ②策略过滤 → ③时间解析 → ④溯源丰富 → ⑤排序交付。
  - **Inv-Scope不变式**：一个检索是作用域安全的当且仅当 \(w_i \in r_j \Rightarrow auth(a_j, s_i, G)\)，即没有智能体收到其身份无权查看的记忆行。
- **算法流程（文字说明）**：论文没有给出具体伪代码，但描述了四个关键原语的操作流程。例如，矛盾检测器在写入后异步运行，检查RDF三元组兼容性并设置接替ID；近重复检测器同步运行，当嵌入相似度超过阈值时返回409拒绝写入。

## 3. 实验设计：数据集/场景、benchmark、对比方法

- **实验场景**：论文使用ArgusFleet框架对MemClaw生产服务（2026年5月30日测量）进行四个维度的评估，每个维度对应一种失效模式：
  - **泄漏实验（Leakage）**：4个舰队（fleet），每个6个智能体，共写入8个秘密（4个舰队作用域+4个智能体作用域）。192次探测。
  - **矛盾实验（Contradiction）**：100个事实（20个不同的主语-谓语模板），每个事实有两个场景（顺序写入+并发写入），共400次写入。
  - **溯源实验（Provenance）**：50条深度为4的推导链（共200次写入），模拟不同操作主题。
  - **传播实验（Propagation）**：40个舰队作用域事实，200次探测（120次同舰队+80次跨舰队）。另加8个专用事实用于测量写入到可见窗口。
- **数据集**：所有内容由确定性种子生成的合成叙事文本（如检查错误率激增、Kafka消费者重平衡循环等）。没有使用标准NLP数据集。
- **Benchmark**：论文没有与基线方法（如MemGPT、Letta、Mem0等）进行对比。其核心贡献在于测量自己的生产服务并暴露设计遗漏，而非比较检索质量。
- **对比方法**：无。作者明确指出“No comparand”，并认为这与贡献一致，因为重点是治理原语的正确性和架构缺陷。

## 4. 资源与算力

- **论文未明确说明**：没有提及GPU型号、数量、训练时长或计算集群配置。MemClaw作为生产服务运行，ArgusFleet作为轻量级Python客户端（基于httpx和tenacity）执行实验，算力需求较低。矛盾实验中写入延迟p99达到19秒，暗示了服务端负载，但未提供底层硬件规格。结论：资源与算力信息缺失。

## 5. 实验数量与充分性

- **实验数量**：
  - 泄漏：192次探测（164预期拒绝+28预期允许），加上96次重新探测（修复后）。
  - 矛盾：200个事实运行（每个有2个写入场景），共400次写入；最终194次成功写入。四个独立\(N=200\)运行以确认稳定性。
  - 溯源：50条深度4的链（200次写入），完成100%重建。
  - 传播：40个事实，200次探测；窗口阶段8个事实，每次写入后250ms间隔轮询。
- **充分性分析**：
  - **优点**：每个实验的样本量统计上合理（如泄漏的Wilson区间为[0.977,1.000]），矛盾检测率基于90/90条件样本，置信区间窄。使用确定性种子和可重现的JSONL痕迹，支持独立验证。
  - **不足**：所有测量在单一租户（freshly provisioned）上进行，跨租户泄漏未测试。工作量规模相对有限（每个实验200次左右），且探测由确定性计划生成而非对抗性构造。时间到可见性的测量仅基于8个事实，分辨率250ms，可能无法捕捉亚250ms的细微延迟。没有与替代记忆服务的对比，使得治理原语的有效性缺乏相对基线。
  - **客观性和公平性**：作者报告了负面的结果（如GET路径绕过、去重门饿死矛盾检测），并公开了已修复的缺陷，增加了可信度。但由于是自评估（同一团队开发ArgusFleet和MemClaw），存在偏倚风险。作者承认这一点并强调负面结果和可重现痕迹作为缓解。

## 6. 论文的主要结论与发现

- **正面结果**：
  - **溯源完整性**：所有50条深度4的链100%重建，每跳延迟p50≈291ms，p95≈491ms。写入者身份正确率100%。
  - **传播正确性**：同舰队可见率97.5%（117/120），跨舰队泄漏率0%（0/80）。强写入模式下写入到可见窗口p50≈0.83s（一次搜索往返），一致性成本发生在写入延迟而非读取延迟。
  - **矛盾接替**：当两个矛盾写入均被接受时，接替正确率为100%（90/90），旧行在~6s内变为outdated。
- **架构缺陷（负面结果）**：
  - **GET路径作用域绕过**：子租户（舰队/智能体）作用域在GET-by-id上未强制检查。即使代理作用域凭据解析了智能体身份，处理器仅检查租户ID。已披露并修复（2026-05-31修复后重测：信任级1跨舰队读均被拒绝）。这违反了Inv-Scope不变式。
  - **去重门饿死矛盾检测**：同步近重复检测器在异步矛盾检测器之前运行，导致自然表达的矛盾写入（“X是A”vs“X是B”）因内容高度相似而被409拒绝，从未触及矛盾检测器。这是流水线排序问题。
  - **跨智能体传播权限过粗**：/redistribute端点要求信任级≥3，将舰队内知识传播与管理性迁移混为一谈，缺乏更细粒度的策略表面。
- **总体结论**：仅靠上下文窗口增大或检索增强不足以应对生产多智能体记忆需求。需要显式的分布式系统级抽象（治理、同步、时序正确性、溯源）。实时评估是暴露执行缺陷和流水线排序问题的必要手段。

## 7. 优点：方法或实验设计上的亮点

- **问题形式化清晰**：给出了群组记忆问题的严格形式化定义（五元组、写入/检索操作、Inv-Scope不变式），并将四个失效模式显式分类，为后续研究提供了理论框架。
- **原语设计完整**：将作用域、时间接替、溯源、策略管控统合在一个架构下，特别是将时间接替作为一等操作（之前的多智能体记忆工作多为隐含）。
- **评估框架可重现**：ArgusFleet开源，提供确定性种子、JSONL事件痕迹、CSV摘要和图表，支持无服务端重新分析。所有报告数字均可从提交的痕迹中再生。
- **实时测量暴露真实缺陷**：通过在真实生产服务上执行定制化探测，发现了仅靠设计审查无法发现的执行缺陷（如GET路径跳过子租户检查）和流水线排序冲突（去重门饿死矛盾检测）。这些负面结果论文主动公开，增强了贡献的诚实性。
- **诊断性分析**：将泄漏分解为安全信号（泄漏率）和可用性信号（缺失率），并区分访问路径和搜索路径。对一致性窗口采用解耦测量（专用轮询）避免了探针调度污染。
- **负责任的披露与修复**：发现的GET路径缺陷在研究期间被披露并得到修复，修复后重测证实了改变，整个过程透明记录。

## 8. 不足与局限

- **自评估偏倚**：ArgusFleet和MemClaw由同一团队开发，缺乏独立审计。虽通过报告负面结果和提交痕迹来缓解，但独立复现（如针对其他兼容服务）会更有说服力。
- **单租户测量**：所有实验在单一新创建的租户中进行，跨租户泄漏表面未被测试。虽然论文声称租户边界在网关上被统一强制，但缺乏实证确认。
- **缺乏对比基线**：没有将MemClaw与MemGPT、Letta、Mem0等现有系统进行对比，无法量化治理原语相对于单智能体检索基础设施的改进程度。
- **工作量规模和多样性有限**：每个实验仅200次左右探测/写入，且合成内容由确定性生成，可能未覆盖边缘情况或对抗性模式。泄漏探测的“预期-拒绝”判定基于信任阶梯模型，但该模型本身是MemClaw的实现细节，未必适用于其他系统。
- **时间到可见性测量粒度粗**：仅8个事实以250ms间隔轮询，可能无法分辨亚250ms的差异，也未在持续负载下测试同步富集行为。
- **目标时效性**：论文发表于2026年，所描述的修复已应用于生产服务，因此部分发现的“开放状态”在发布时已不可重现，依赖当时的手工记录。
- **缺乏公平性讨论**：虽然承认了偏倚，但未分析报告负面结果可能带来的激励结构（如“刻意制造缺陷以显深度”），尽管公开修复行为部分削弱这一质疑。

（完）
