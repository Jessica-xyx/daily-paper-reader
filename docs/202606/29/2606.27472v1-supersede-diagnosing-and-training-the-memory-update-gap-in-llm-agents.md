---
title: "Supersede: Diagnosing and Training the Memory-Update Gap in LLM Agents"
title_zh: Supersede：诊断与训练大语言模型智能体中的记忆更新缺口
authors: Vedant Patel
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.27472v1"
tags: ["query:llm-agent-memory"]
score: 10.0
evidence: 诊断并训练LLM智能体记忆更新失败问题
tldr: "LLM智能体在多会话交互中需追踪事实变化，但现有模型在记忆更新上存在显著缺陷。在LongMemEval测试中，使用受限记忆的gpt-5.4准确率从92%骤降至77%，且随对话长度增加而持续下降，扩大记忆容量无效。为诊断并训练此能力，作者构建了Supersede强化学习环境，基于GRPO微调Qwen2.5-3B模型，使未见对话上的超期准确率从9.0%提升至16.7%。该工作是首个针对时间性事实时效性的可训练环境，证明记忆更新鸿沟可被针对性训练弥合。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27472v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 941, \"height\": 336, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27472v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1215, \"height\": 742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27472v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1213, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.27472v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1212, \"height\": 550, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.27472v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1144, \"height\": 221, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27472v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1050, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27472v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.27472v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1307, \"height\": 618, \"label\": \"Table\"}]"
motivation: LLM智能体在多会话交互中无法正确使用最新事实，存在记忆更新鸿沟，且无法通过扩大模型或记忆容量解决。
method: 构建Supersede强化学习环境，使用GRPO算法微调Qwen2.5-3B模型，通过奖励当前值回答、惩罚陈旧值来训练事实时效性。
result: "在真实对话测试上，微调模型超期准确率从9.0%提升至16.7%，且沿单调检查点曲线提升，证明策略学习有效。"
conclusion: 记忆更新鸿沟是可通过针对性训练弥合的，Supersede为首次提供此类训练环境与证据。
---

## 摘要
大语言模型（LLM）智能体在长周期、多会话的交互中运行，其中事实会发生变化：用户搬家、价格更新、计划修订。正确行动需要使用事实的当前值，并丢弃已被取代的值。我们在真实对话数据上分离出这一能力，并表明这是一个独特的、未解决的失败。在LongMemEval的知识更新子集上，即使在前沿模型（gpt-5.4）上，用有限的自维护记忆替换智能体的完整上下文也会将准确率从92%降至77%，这一差距具有统计显著性（配对McNemar检验p<0.005），并且在模型规模增加时持续存在，而完整上下文准确率接近92%的饱和点。因此，瓶颈在于记忆维护而非理解，并且无法通过更强的模型来弥补。我们接着探究这是否仅仅是由于记忆容量不足，结果发现并非如此：随着对话增长24倍，准确率进一步下降（从68%降至28%），而给智能体成比例更多的记忆并未带来可检测的恢复（28%至28%，n=25）。失败与对话长度成比例，而非压缩比。我们发布Supersede，一个开放强化学习环境（基于verifiers/prime-rl栈），将这一测量转化为训练信号：智能体因基于当前值回答而获得奖励，因使用过时值而受到惩罚。最后，我们闭合循环并证明该缺口是可训练的：在此环境上对一个小型开放模型（Qwen2.5-3B）进行GRPO微调，其在真实未见对话上的保留取代准确率几乎翻倍（9.0%至16.7%，单次运行），沿单调的检查点曲线表明学习到的策略（而非工具支撑）带来了增益。据我们所知，这是第一个奖励目标为时间事实货币性的可训练环境，也是第一个表明取代缺口可以被训练缩小而不仅是被测量的证据。

## Abstract
Large language model (LLM) agents operate over long, multi-session interactions in which facts change: a user moves, a price updates, a plan is revised. Acting correctly requires using the current value of a fact and discarding values that have been superseded. We isolate this ability on real conversational data and show that it is a distinct, unsolved failure. On the knowledge-update subset of LongMemEval, replacing an agent's full context with a bounded, self-maintained memory drops accuracy from 92% to 77% even on a frontier model (gpt-5.4), a gap that is statistically significant (paired McNemar p<0.005) and persists across model scale while full-context accuracy saturates near 92%. The bottleneck is therefore memory maintenance, not comprehension, and is not closed by a stronger model. We then ask whether this is merely an undersized memory, and find it is not: as the conversation grows 24x, accuracy falls further (from 68% to 28%), and granting the agent proportionally more memory yields no detectable recovery (28% to 28%, n=25). The failure scales with the length of the conversation, not the compression ratio. We release Supersede, an open reinforcement-learning environment (on the verifiers / prime-rl stack) that turns this measurement into a training signal: agents are rewarded for answering from the current value and penalized for stale ones. Finally, we close the loop and show the gap is trainable: GRPO fine-tuning a small open model (Qwen2.5-3B) on this environment nearly doubles its held-out supersession accuracy on real, unseen conversations (9.0% to 16.7%, a single run), along a monotonic checkpoint curve indicating the learned policy, not the harness, carries the gain. To our knowledge this is the first trainable environment whose reward targets temporal fact-currency, and the first evidence the supersession gap can be trained down, not only measured.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLM）智能体在多会话、长周期交互中，事实会动态变化（如用户搬家、价格更新、计划修订），但模型无法正确使用当前值并丢弃已过时（superseded）的值，存在“记忆更新鸿沟”（supersession gap）。
- **背景**：现有研究（如LoCoMo、LongMemEval）仅评估该问题而未提供训练方案；强化学习用于记忆管理的工作（如MemAgent、LongRLVR）奖励的是任务正确性或证据相关性，而非时间性事实货币性。封闭的生产系统（OpenAI Dreaming、Claude memory）自报成功率低，且缺乏可训练的开放环境。
- **整体含义**：证明该失败是记忆策略问题而非理解问题，无法通过扩大模型或记忆容量解决；首次提出可训练的强化学习环境，以时间性事实货币性为奖励，训练模型弥合该鸿沟。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：构建一个强化学习环境 `Supersede`，智能体在维护有限自维护记忆（bounded memory）的过程中，必须学习保留事实的当前值、丢弃过时值；奖励直接基于答案是否反映当前值，惩罚基于过时值。
- **关键技术细节**：
  - **任务设计**：多会话交互 → 最终查询。智能体一次处理一个会话，维护一个限制为 `B` 字符的笔记字段（notes）；原始会话永不重新输入。最终只能从记忆回答。
  - **奖励函数**：  
    `r = r_cur + λ * r_stale`  
    `r_cur = 1[答案传达当前值]`（程序化匹配器，支持归一化、变体、子串和 token 重叠回退）  
    `r_stale = -1[答案断言过时值]`（仅已知过时值时生效）。
  - **数据**：真实对话来自 LongMemEval 知识更新子集 + 程序化生成器（作为训练课程和控制）。
- **算法流程**：
  1. 智能体按顺序处理每个会话，每次重写记忆笔记（最多 B 字符）。
  2. 所有会话处理完后，基于当前记忆回答查询。
  3. 用程序化匹配器评分，给出奖励。
  4. 使用 GRPO（Group Relative Policy Optimization）训练：从一组采样 rollout 估计基线，无需值批评器；使用 LoRA 适配器微调 Qwen2.5-3B。

### 3. 实验设计：数据集 / 场景、基准测试、对比方法

- **数据集**：
  - **诊断**：LongMemEval 知识更新子集，分为 `oracle` 分片（约 2 个会话/问题，78 题）和 `_s` 分片（约 48 个会话/问题，长历史）。
  - **训练**：程序化生成器（6-8 会话，一个事实被后来取代，含分心会话），2000 个 episode，与真实测试集无重叠。
- **基准测试（Benchmark）**：LongMemEval 知识更新子集的标准评估协议（LLM 评判或程序化匹配器）。
- **对比方法**：
  - **模型规模对比**：gpt-4.1-mini、gpt-4.1、gpt-5.4（温度0）。
  - **记忆容量对比**：固定 300 字符记忆 vs. 成比例放大记忆（约 7150 字符）。
  - **训练对比**：Qwen2.5-3B 基模型 vs. GRPO 微调后的模型。
  - **条件对比**：Full-Context（上下文中含所有会话） vs. Bounded-Memory（仅有限记忆）。

### 4. 资源与算力

- **训练**：4 × A10G GPU；使用 GRPO + LoRA（rank=32, α=64）；batch size 32，group size 8；学习率 10⁻⁵；训练直到自终止（step 175）。  
- **训练任务**：2000 个程序化生成的 episode。  
- **文中未明确说明训练总时长**，仅给出步数。推理使用温度 0 的贪婪解码。

### 5. 实验数量与充分性

- **诊断实验**（Section 5.1）：三种模型 × 两个条件（Full-Context vs. Bounded-Memory），n=78，配对 McNemar 检验 p < 0.005。  
- **规模研究**（Section 5.2）：gpt-4.1-mini，n=25（因 _s 分片较小）；三个条件：oracle（2会话/300记忆）、_s（48会话/300记忆）、_s（48会话/7150记忆）。  
- **训练实验**（Section 5.3）：Qwen2.5-3B 单次 GRPO 运行，检查点步数：50, 100, 150, 175（自终止）。在 LongMemEval oracle 分片上评估，n=78。  
- **消融/控制**：程序化匹配器 vs LLM 评判器的一致性检查；合成数据饱和性验证；环境端到端验证。  
- **充分性评价**：诊断部分实验充分（模型规模、记忆容量双轴验证）；训练实验仅单次运行，未做多种子统计显著性检验（作者指出为 ongoing work）。规模研究 n=25 较小，但差异显著。整体实验设计客观，通过配对检验和固定匹配器保证公平性。

### 6. 论文的主要结论与发现

- **主要结论**：记忆更新鸿沟存在且显著，无法通过更大模型或更大记忆容量缓解，但可以通过针对性强化学习训练来缩小。
- **具体发现**：
  1. **Bounded memory 显著降低超期准确率**：gpt-5.4 从 92% 降至 77%（p=0.0033）。
  2. **模型规模不解决问题**：加大模型仅略微提升 bound memory 准确率（63%→77%），但 gap 仍大。
  3. **记忆容量不解决问题**：对话增长 24 倍，准确率从 68% 降至 28%；增加记忆至等比例 (24x) 毫无提升（28%）。
  4. **训练解决问题**：Qwen2.5-3B 经过 GRPO 微调，在真实未见数据上超期准确率从 9.0% 升至 16.7%（+86%），且提升单调。
- **核心洞察**：该失败是记忆维护策略问题，而非理解问题。

### 7. 优点：方法或实验设计上的亮点

- **诊断与训练闭环**：先精确测量 gap，再证明其可训练，逻辑完整。
- **实验设计干净**：
  - 使用配对 McNemar 检验，控制个体差异。
  - 独立验证模型规模与记忆规模两个维度，排除常见猜测。
  - 程序化匹配器与 LLM 评判器交叉验证，避免自我评分偏差。
  - hold-out 评估使用完全不同来源数据，确保是策略迁移而非记忆化。
- **环境设计创新**：
  - 首将事实货币性（temporal fact-currency）作为直接奖励信号，而不是任务正确性代理。
  - 禁用重读历史，迫使模型依赖自身记忆维护，模拟真实生产环境。
  - 程序化匹配器避免辅助评判模型带来的额外开销和偏见。
- **训练可重复性**：单一性种子但给出单调检查点曲线，证明学习稳健而非侥幸。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **训练实验局限**：
  - 仅使用单个小模型（Qwen2.5-3B），绝对准确率仍很低（16.7%）。
  - 仅单次运行，未报告多种子统计显著性（作者承认 ongoing work）。
  - 程序化训练课程饱和（self-terminate），无法学习更难的超期场景。
- **诊断实验局限**：
  - 规模研究 n=25 较小，且“scale”轴主要增加分心会话而非同一事实的多次更新。
  - 不同实验部分使用不同评分器（Section 5.1 用 LLM 评判，Section 5.2 和 5.3 用程序化匹配器），绝对数字不可直接比较。
  - 程序化匹配器对表面形式宽松，可能高估绝对准确率；但预/后对比不受影响。
- **泛化性有限**：
  - 仅针对单一任务类型（事实更新查询），未测试其他记忆操作（删除、合并等）。
  - 仅测试了特定模型族（GPT-4, Qwen2.5），未扩展到其他架构。
- **应用限制**：
  - 环境基于固定字符限制记忆，未考虑更复杂的记忆结构（向量数据库、结构化笔记）。
  - 训练奖励仅考虑当前值，未处理多值（例如同时维护多个版本）或不确定性。

（完）
