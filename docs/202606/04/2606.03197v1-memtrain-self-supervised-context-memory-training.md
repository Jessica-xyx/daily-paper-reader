---
title: "MemTrain: Self-Supervised Context Memory Training"
title_zh: MemTrain：自监督上下文记忆训练
authors: "Ziheng Li, Xingrun Xing, Haoqing Wang, Zhi-Hong Deng, Yehui Tang"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03197v1"
tags: ["query:llm-agent-memory"]
score: 9.0
evidence: 面向LLM智能体的自监督上下文记忆训练
tldr: 长时间交互中LLM智能体需要记忆能力，但现有方法依赖昂贵下游标注且训练数据多样不足。MemTrain提出基于未标注维基百科的自监督框架，通过掩码重建和中间记忆回忆双任务联合优化，无需下游标注即可提升记忆能力。在长文本和检索QA任务上，相比直接下游训练取得最高17.67点提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03197v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1373, \"height\": 577, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03197v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1381, \"height\": 579, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03197v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1368, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.03197v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1126, \"height\": 701, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03197v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1440, \"height\": 451, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03197v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1451, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03197v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1455, \"height\": 1193, \"label\": \"Table\"}]"
motivation: 现有记忆智能体端到端训练依赖昂贵下游标注，数据多样性不足，难以泛化通用记忆行为。
method: 在未标注语料上设计掩码重建与中间记忆回忆两个代理任务，通过GRPO联合优化模型记忆维护与压缩能力。
result: 在长文本和检索QA基准上，MemTrain比直接下游训练一致提升，最高增益17.67点。
conclusion: 自监督记忆训练能有效增强LLM智能体通用记忆能力，为下游任务提供更好基础。
---

## 摘要
记忆是长程大语言模型代理不可或缺的能力，使其能够保存和利用在长交互过程中积累的信息。现有的记忆代理方法通常在下游任务上使用强化学习进行端到端训练。然而，为记忆密集型场景收集高质量的标注问题成本高昂，且生成的训练数据往往缺乏足够的多样性以覆盖通用的记忆行为。在这项工作中，我们提出了MemTrain，一个自监督训练框架，用于普遍增强大语言模型代理的上下文记忆能力，以实现更有效的下游后训练。MemTrain在未标记的维基百科语料上引入了两个耦合的代理任务：（1）端到端掩码重建目标，要求模型在多次记忆更新后恢复被掩码的实体，从而从最终结果角度促进记忆维护；（2）中间记忆召回目标，要求模型使用中间记忆状态重建被掩码的历史信息，促进整个交互过程中的忠实压缩和记忆完整性。这两个目标通过GRPO联合优化。在长文本问答和基于搜索的问答基准上的大量实验表明，MemTrain在不同模型上一致地提高了下游记忆密集型推理性能，相较于直接进行任务特定后训练，获得了高达17.67个点的提升。

## Abstract
Memory is an indispensable capability for long-horizon LLM agents, enabling them to preserve and utilize information accumulated across extended interactions. Existing memory-agent approaches are typically trained end-to-end with reinforcement learning on downstream tasks. However, collecting high-quality annotated problems for memory-intensive scenarios is costly, and the resulting training data often lack sufficient diversity to cover general memory behaviors. In this work, we propose MemTrain, a self-supervised training framework for generally enhancing the context-memory capability of LLM agents for more effective downstream post-training. MemTrain introduces two coupled proxy tasks over unlabeled Wikipedia corpora: (1) an end-to-end masked reconstruction objective, which requires the model to recover masked entities after multiple rounds of memory updates, thereby encouraging memory maintenance from the final outcome perspective; and (2) an intermediate memory recall objective, which requires the model to reconstruct masked historical information using intermediate memory states, encouraging faithful compression and memory completeness throughout the interaction process. The two objectives are jointly optimized using GRPO. Extensive experiments on long-text QA and search-based QA benchmarks demonstrate that MemTrain consistently improves downstream memory-intensive reasoning performance across different models, achieving gains of up to 17.67 points over direct task-specific post-training.

---

## 论文详细总结（自动生成）

# 详细论文总结：MemTrain: Self-Supervised Context Memory Training

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：大型语言模型（LLM）在长程交互任务中需要记忆能力来保存和利用历史信息。现有记忆智能体方法通常在下游任务上使用强化学习进行端到端训练，但这需要大量高质量标注数据，成本高且数据多样性不足，导致学到的记忆能力领域特定、泛化性差。
- **整体含义**：本文旨在提出一种无需标注的自监督训练框架，以普遍提升LLM智能体的上下文记忆能力，从而为下游任务后训练提供更好的初始化，降低对昂贵标注数据的依赖，提升长程交互性能。

## 2. 提出的方法论
### 核心思想
- 基于**未标注的维基百科语料**设计两个耦合的代理任务，通过GRPO（Group Relative Policy Optimization）联合优化，使模型学会维护和利用中间记忆状态。

### 关键技术细节
- **问题设定**：基于MemAgent提出的多轮上下文记忆框架。模型在每一交互步维护固定长度的记忆状态 \(m_t\)，接收输入 \((m_{t-1}, a_{t-1}, i_t)\)，输出新记忆和动作。
- **端到端掩码重建任务**：
  - 从维基百科随机选取一个段落作为“枢轴段落”，检索29个语义相关段落并混合120个随机段落，构建长文档（24k-40k tokens）。
  - 从枢轴段落中随机选取一个实体，将所有出现替换为 `[MASK]`。
  - 模型依次读取分段后的文档块（每块约5k tokens），更新记忆，最后基于最终记忆预测被掩码实体。该任务从**最终结果**角度激励记忆维护。
- **中间记忆回忆任务**：
  - 在端到端轨迹中随机选取一个中间步骤 \(k\)，将对应的记忆状态 \(o_{i,k}\) 与一个早先的掩码块 \(\tilde{c}_l\) 拼接，要求模型在单步内恢复该块中被掩码的实体。
  - 该任务从**过程监督**角度鼓励记忆的忠实压缩和完整性。
- **联合优化**：
  - 每个训练样本生成 \(G_1=8\) 条端到端轨迹，对每条再采样 \(G_2=8\) 条中间回忆轨迹。
  - **奖励设计**：中间回忆奖励为精确匹配（1/0）；端到端奖励 = 最终预测精确匹配 + \(\lambda \times\) 平均中间回忆奖励（\(\lambda=0.5\)）。
  - **优势计算**：采用未归一化优势 \( \hat{A}_{i,j,k} = R_i - \text{mean}\{R_i\}_{i=1}^{G_1} \)，广播至所有交互步。
  - **训练算法**：GRPO，配合KL正则化（系数 \(1\times10^{-3}\)），并过滤全零或全一的样本（DAPO策略）。上下文长度限制为8192 tokens（含指令1024、输入块5120、记忆1024、响应1024）。

## 3. 实验设计
### 数据集与场景
- **记忆训练数据**：使用通用维基百科作为无监督语料，生成30k个训练文档（长度24k-40k tokens）。
- **下游任务**：
  - **长文本多跳问答**：使用Yu et al. (2025b)提出的HotpotQA变体，文档长度从7k到896k tokens不等。
  - **基于搜索的多跳问答**：7个挑战性基准：TriviaQA、Bamboogle、HotpotQA、Natural Questions、PopQA、2WikiMultiHopQA、MusiQUE。

### 对比方法
- 基线模型：原始指令模型（Qwen3-4B-Instruct-2507和Qwen2.5-7B-Instruct）。
- 仅记忆训练（+MemTrain）：仅应用MemTrain的自监督训练。
- 仅下游后训练（+MemAgent或+MEM1）：直接应用对应的下游算法。
- 组合方法（MemTrain+MemAgent或MemTrain+MEM1）：先进行MemTrain再应用下游后训练。

### 评估设置
- 长文本QA：使用MemAgent作为下游算法，训练500步（batch size 32，update batch 8，lr 1e-6），报告不同长度下的准确率。
- 搜索QA：使用MEM1作为下游算法，训练200步（batch size 32，update batch 8，lr 5e-7），报告各基准精确匹配平均分。

## 4. 资源与算力
- **文中未明确说明**具体使用的GPU型号、数量及训练时长。仅提及训练步骤数（300步记忆训练，500步长文本后训练，200步搜索后训练）、批量大小和学习率。因此无法准确评估算力开销。

## 5. 实验数量与充分性
- **实验数量**：涉及两个主要下游任务，每个任务包含多个数据集（长文本1个基准8个长度设置；搜索7个基准）。此外还进行了：
  - **消融实验**：3个变体（Full、End-to-End only、Decoupled），在长文本任务所有长度上对比。
  - **后训练缩放对比**：将MemTrain+MemAgent与继续训练MemAgent 300步对比。
  - **案例研究**：展示具体记忆生成差异。
- **充分性与公平性**：
  - 实验覆盖范围较广，包括不同模型规模（4B、7B）、不同任务类型、不同输入长度。
  - 消融实验设计合理，验证了两个代理任务各自的贡献。
  - 后训练缩放对比表明MemTrain提升了性能天花板，非简单增加步数可替代。
  - 但消融仅在长文本任务上验证，搜索QA任务未做消融；且仅使用两个模型系列，在更大模型或更多架构上的泛化性未验证。

## 6. 主要结论与发现
- MemTrain**一致且显著提升**下游任务性能：在长文本QA上平均提升5.17%（4B模型）和17.67%（7B模型）；在搜索QA上平均提升10.58%（4B）和8.50%（7B）。
- **长度泛化能力**：尽管训练上下文长度约32k-40k，MemTrain+MemAgent在7k到896k所有长度上均优于直接后训练，长序列性能下降更小。
- **自监督有效**：仅MemTrain（无下游标注）即可赋予模型显著的多轮问答能力（如4B模型从21.97%提升至56.15%）。
- **IMR任务关键**：消融表明移除中间回忆任务导致平均分下降约7%；解耦奖励在长序列上性能退化，表明联合设计的重要性。
- **性能天花板提升**：即使延长后训练步数，MemTrain初始化的模型仍优于直接后训练的最佳模型，表明其提升了学习上限。

## 7. 优点
- **自监督无需标注**：利用维基百科生成代理任务，大幅降低数据收集成本，提升数据多样性。
- **双任务耦合设计**：从结果和过程两个层面提供监督信号，既促进最终记忆利用，又保证中间记忆质量。
- **长度泛化性强**：训练后对远超训练长度的输入仍能保持较好性能，表明学到的是通用记忆行为而非过拟合训练分布。
- **即插即用**：可无缝集成到现有的下游后训练框架（如MemAgent、MEM1）中，提升性能天花板。
- **实验全面**：在多种任务、多种模型规模、多种长度下验证，并包含深入消融和缩放分析。

## 8. 不足与局限
- **算力资源未报告**：缺失硬件配置和训练时间，不利于评估方法的实际部署成本。
- **消融实验不完全**：仅对长文本任务进行消融，搜索QA任务未评估各组件贡献；且仅分析了两个变体，未检查超参数（如λ、G1/G2）的敏感性。
- **模型规模有限**：仅测试4B和7B模型，在更大模型（如70B）或更多架构（如LLaMA）上的效果未知。
- **记忆训练成本**：自监督代理任务需要额外的训练步骤（300步），且涉及多次采样（G1=8, G2=8），计算开销可能较大，但论文未与直接后训练的计算成本对比。
- **应用限制**：任务构造依赖于维基百科实体，可能无法完美匹配所有下游场景的记忆需求；此外，记忆更新机制假设固定长度上下文，可能不适用于需要可变记忆大小的复杂环境。
- **潜在偏差风险**：只基于维基百科语料，可能存在语言、领域偏差；下游搜索QA评估基于特定检索器和数据库，泛化至真实搜索场景需进一步验证。

（完）
