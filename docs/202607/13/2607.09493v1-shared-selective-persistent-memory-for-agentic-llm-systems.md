---
title: Shared Selective Persistent Memory for Agentic LLM Systems
title_zh: 共享选择性持久化记忆：面向智能体LLM系统
authors: "Sanjana Pedada, Aditya Dhavala, Neelraj Patil"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09493v1"
tags: ["query:llm-agent-memory"]
score: 9.0
evidence: 为智能体LLM系统设计的共享选择性持久记忆架构
tldr: "多轮交互的LLM智能体系统面临会话上下文重置与历史无关的困境，简单保留完整历史不仅低效且会降低生成质量。提出共享选择性持久记忆架构，识别并保留任务规范、数据模式、工具配置、输出约束四类可复用上下文，丢弃推理痕迹。在协作工作台平台实现，支持跨用户共享与角色控制。引入零令牌数据刷新机制，分离生成程序与运行时数据。在三个企业场景中任务完成率达96%，远超无记忆的79%和全历史的71%；零令牌刷新减少14倍任务时间，摘要生成降低97倍令牌成本。在四个公开数据集上复现验证泛化性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09493v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 774, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09493v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 817, \"height\": 607, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09493v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 278, \"height\": 133, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09493v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 773, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09493v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 775, \"height\": 284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09493v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 737, \"height\": 307, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09493v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 836, \"height\": 467, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09493v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 824, \"height\": 866, \"label\": \"Table\"}]"
motivation: 多轮工具使用的LLM系统每次会话从零开始，丢弃之前高效配置与模式，而保存全部历史既浪费令牌又因无关信息降低生成质量。
method: 提出共享选择性持久记忆架构，保留任务规范、数据模式、工具配置和输出约束四类可复用上下文，丢弃推理痕迹；支持跨用户共享与角色控制，并含零令牌数据刷新机制。
result: "三个企业场景任务完成率96%（无记忆79%，全历史71%），零令牌刷新减少14倍任务时间，摘要生成比原始数据注入降低97倍令牌成本，公开数据集12/12成功。"
conclusion: 共享选择性持久记忆显著提升多轮LLM智能体系统的效率与效果，全历史记忆因陈旧痕迹反而有害，选择性记忆优于两种极端。
---

## 摘要
通过多轮工具使用生成代码的智能体LLM系统面临一个根本性的上下文问题：每次会话从零开始，丢弃了先前会话中有效的配置选择、领域约束、数据模式和工具使用模式。简单持久化整个对话历史在token效率上低下且适得其反：无关上下文会降低生成质量。我们提出共享选择性持久化记忆架构，该架构识别并保留四类可复用上下文（任务规范、数据模式、工具配置和输出约束），同时丢弃会话特定的推理痕迹。关键在于，这种记忆是共享的：封装了选择性记忆的工作区可以通过基于角色的访问控制在不同用户间转移，从而无需冗余规范即可实现协作复用。我们在一个已部署的协作工作区平台上实现了该架构，LLM智能体从异构数据源（CSV、SQL、REST API和MCP服务器）生成、编辑和维护基于Git版本控制的工件（仪表板、报告和数据驱动文档）。一种互补的零token数据刷新机制将生成的程序与运行时数据解耦，使工件无需重新调用即可复用。在三个企业场景中，共享选择性持久化记忆实现了96%的任务完成率（相比之下，无记忆为79%，完整历史为71%）。零token刷新消除了针对重复更新的LLM重新调用（任务时间减少14倍），而摘要驱动的生成将每次调用的token成本比原始数据注入降低了97倍。在四个公共数据集上的复制实验证实了其泛化能力，零token刷新在12/12次试验中成功。值得注意的是，朴素的完整历史持久化会通过过时痕迹偏向智能体而主动降低完成率，而选择性记忆则优于两个极端。

## Abstract
Agentic LLM systems that generate code through multi-turn tool use face a fundamental context problem: each session starts from zero, discarding the configuration choices, domain constraints, data schemas, and tool-use patterns that made previous sessions productive. Naively persisting entire conversation histories is token-inefficient and counterproductive: irrelevant context degrades generation quality. We introduce shared selective persistent memory, an architecture that identifies and retains four categories of reusable context (task specifications, data schemas, tool configurations, and output constraints) while discarding session-specific reasoning traces. Crucially, this memory is shared: workspaces encapsulating selective memory can be transferred across users with role-based access control, enabling collaborative reuse without redundant specification. We implement it in a deployed collaborative workspace platform where LLM agents produce, edit, and maintain git-versioned artifacts (dashboards, reports, and data-driven documents) from heterogeneous sources (CSV, SQL, REST APIs, and MCP servers). A complementary zero-token data refresh mechanism decouples generated programs from runtime data, enabling artifact reuse without re-invocation. Across three enterprise scenarios, shared selective persistent memory achieves 96% task completion (vs. 79% without memory and 71% with full history). Zero-token refresh eliminates LLM re-invocation for recurring updates (14x task-time reduction), while summary-driven generation cuts per-invocation token cost by 97x versus raw data injection. A replication on four public datasets confirms generalizability, with zero-token refresh succeeding in 12/12 trials. Notably, naive full-history persistence actively degrades completion by biasing the agent with stale traces, while selective memory outperforms both extremes.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **背景**：智能体 LLM 系统通过多轮工具使用自动生成代码（如仪表盘、报告），但现行框架**会话无状态**：每次会话从零开始，丢弃先前积累的配置、领域约束、数据模式、输出格式偏好等可复用知识。
- **核心问题**：简单持久化整个对话历史不仅 token 效率低下，而且**有害**——无关上下文导致“迷失在中间”效应，降低生成质量；过去的推理痕迹会**误导**智能体重复旧路径而非适应当前任务。
- **研究目标**：设计一种**选择性持久化记忆**架构，只保留高价值的可复用知识，丢弃会话特定的推理痕迹，并支持跨用户共享，从而将单次会话转化为持久、协作、可复用的工作空间。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：将可复用知识分解为四个正交类别，持久化这些紧凑、跨会话稳定的结构化信息，同时**选择性遗忘**所有会话内中间过程。
- **四类持久化记忆**：
  - **任务规范 ( \(M_{\text{task}}\) )**：用户编写的领域规则、输出偏好、质量约束（如“达标≥95%用绿色”）。
  - **数据模式 ( \(M_{\text{data}}\) )**：自动从数据源预计算的统计摘要（列名、类型、分布、唯一值目录等），不包含原始行数据，通常仅需约 500 token。
  - **工具配置 ( \(M_{\text{tools}}\) )**：可用外部工具的参数模式、调用方式、认证要求（如 SQL 连接、REST 端点、MCP 服务器）。
  - **输出约束 ( \(M_{\text{output}}\) )**：生成程序与运行时环境间的契约，核心是**数据注入契约**——程序必须从运行时注入点消费数据，不得硬编码。
- **选择性遗忘内容**：中间文件、未经用户批准的修改、LLM 推理链、工具调用日志、错误恢复路径、原始数据。这避免了“迷失在中间”和路径偏见。
- **会话开始时的记忆组合**：
  - 系统提示：\( s' = s_{\text{base}} \oplus M_{\text{task}} \oplus M_{\text{tools}} \oplus M_{\text{output}} \)
  - 用户消息：\( q' = q \oplus M_{\text{data}} \)
  - 组合在应用层完成，LLM 无感知。
- **零 token 数据刷新机制**：生成的工件严格遵循数据注入契约，新数据到达时检查模式兼容性（原始列集是新源的子集），若兼容则无需重新调用 LLM，数据自动刷新，零 token 消耗。支持增量模式演化（允许新增列，缺失列则触发重新生成）。
- **协作共享**：工作区封装完整选择性记忆和版本历史，通过 OIDC 角色（所有者、管理员、查看者）共享，支持他人加载后换数据、细化、查询。

### 3. 实验设计：数据集/场景、基准、对比方法
- **数据集**：
  - **企业数据**：24 个真实文件，涵盖供应链、销售、流程指标，大小从 200 行/8 列到 45K 行/42 列，通过 CSV 和 SQL 连接器引入。
  - **公开数据集**（4 个）：Superstore Sales（1K 行，零售）、UCI Adult Income（32K 行，人口普查）、NYC 311 Service Requests（2K 行，政府）、World Bank GDP（14K 行，经济）。
- **场景/用例**（来自 §1.1 的三个企业场景）：
  - 用例 1（周期性刷新）：10 个任务。
  - 用例 2（跨团队分享）：8 个任务。
  - 用例 3（迭代构建）：6 个任务。
- **基准与对比方法**（实验 1 条件消融）：
  - **无记忆**：默认提示，每次完全重设。
  - **完整历史**：将前一次完整对话注入新会话。
  - **选择性记忆**：仅保留四类结构化记忆，丢弃会话痕迹。
- **评价指标**：两位盲评者（κ=0.91）根据通过/不通过标准评分（渲染正确性、数据保真度、格式合规、完整性）；还统计了用户轮次、输入/输出 token 数、时间。
- **实验 2**（公开数据集复制）：4 个数据集 × 3 条件 × 3 轮次 = 36 次试验，通过自动化检查验证。
- **实验 3**（token 效率）：比较原始数据注入、截断前 50 行、摘要驱动三种数据表示策略。
- **实验 4**（用户研究）：12 名参与者（6 名工程师，6 名分析师），4 个反平衡任务，测量时间、李克特量表。

### 4. 资源与算力
- **文中未明确说明 GPU 型号、数量、训练时长**。实验使用已部署的系统，LLM 推理采用 Claude Opus 4（主要生成）和 Claude Sonnet 4（轻量子任务）。系统为推理服务，非训练，因此无训练算力需求。作者也未披露推理的具体硬件配置。

### 5. 实验数量与充分性
- **实验数量**：
  - 实验 1（企业消融）：24 个任务。
  - 实验 2（公开数据集）：4 数据集×3 条件×3 轮=36 次。
  - 实验 3（token 效率）：在不同数据上比较三种表示。
  - 实验 4（用户研究）：12 人，4 任务。
- **充分性与客观性**：
  - 盲评、双人打分、高一致性（κ=0.91）。
  - 使用 Fisher 确切检验验证显著性（选择性 vs 无记忆 p=0.046；选择性 vs 完整历史 p=0.008）。
  - 覆盖企业实际场景和多样公开数据，任务类型全面。
  - **用户研究样本量较小（N=12）**，不足以对单个 Likert 项做统计推断，但足以识别趋势。
  - 总体实验设计充分、客观，对比方法设置合理，消融控制充分。

### 6. 论文的主要结论与发现
- **选择性记忆显著优于无记忆和完整历史**：任务完成率 96% vs 79% vs 71%。
- **完整历史持久化是反效果**：比无记忆更差（+9× token，完成率下降 8pp），因为陈旧推理痕迹导致路径锚定。
- **零 token 刷新在 12/12 公开数据集试验中成功**，任务时间减少 14×（无需 LLM 调用）。
- **摘要驱动生成降低 token 成本**：企业数据 97× 降低，公开数据 946× 降低，且生成质量无损。
- **协作共享有效**：跨用户角色控制的场景中，无需重设即可复用上下文。
- **关键启示**：上下文管理（而非模型能力）是提升企业智能体 LLM 效率的主要杠杆。

### 7. 优点
- **方法创新**：提出四类结构化记忆分类和选择性遗忘原则，解决了“完整历史有害”的关键问题。
- **实用性强**：零 token 刷新机制显著降低重复操作的成本；Git 版本控制、草稿隔离、多连接器集成让系统可直接部署。
- **实验设计严谨**：盲评、统计检验、覆盖企业数据和公开数据、多角度分析（完成率、token、时间、用户反馈）。
- **可复现性**：公开数据集复制实验增强结果可信度。
- **协作特性**：工作区共享、基于角色的细粒度权限管理，面向真实多用户协作场景。

### 8. 不足与局限
- **记忆分类手工设计**：当前四大类别是作者基于经验定义的，未探索自动识别可复用上下文的方法。
- **零 token 刷新适用范围有限**：仅适用于结构化表格数据（稳定模式），不支持流数据、非结构化文档或实时 API。
- **用户研究样本量小**（N=12），虽可识别趋势但缺乏统计效力，未做力分析。
- **协作共享粒度粗**：仅支持工作区级共享，不支持独立记忆成分（如仅共享工具配置而不共享任务规范）的细粒度共享。
- **未处理算力与延迟细节**：未说明模型推理的具体硬件和响应时间分布（仅给出均值）。
- **多连接器可靠性问题**：各连接器（SQL、REST、MCP）有独立故障模式（超时、认证过期、模式漂移），缺乏统一重试策略。
- **评价主观维度缺失**：仅基于功能正确性，未评估视觉美学、信息层次等主观质量。
- **扩展方向提及但未解决**：如学习式记忆选择、记忆组合、记忆衰减、智能体级别持久记忆等。

（完）
