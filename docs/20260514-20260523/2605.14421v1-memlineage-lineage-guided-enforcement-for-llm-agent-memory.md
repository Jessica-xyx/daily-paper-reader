---
title: "MemLineage: Lineage-Guided Enforcement for LLM Agent Memory"
title_zh: "MemLineage: 用于LLM代理内存的血缘引导强制执行"
authors: "Ciyan Ouyang, Rui Hou"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14421v1"
tags: ["query:llm-agent-memory"]
score: 7.0
evidence: 利用血统的代理记忆管理防御机制
tldr: LLM代理内存易受中毒攻击，不可信内容可被持久化并作为指令重新注入。MemLineage将其视为链式监管问题，通过为每个内存条目附加密码学来源和派生谱系，构建基于Merkle日志和加权DAG的防御机制。在多种评估中，它将攻击成功率降至零，且每次操作开销低于毫秒，远低于LLM调用噪声，同时保持良性回忆。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14421v1/fig-001.webp\", \"caption\": \"Figure 8: Four-dimensional capability matrix. Crypto integrity for the trust label, lineage attribution across LLM-mediated derivation, cross-session persistence of the label, and coverage of agent-derived entries. ✓ = full coverage; ⃝ = partial; × = absent. Among the published and concurrent systems compared in this figure, MemLineage’s highlighted row is the only one that covers all four capabilities in one system. Peer rows are coded from their published mechanisms rather than local reproductions.\", \"page\": 21, \"index\": 1, \"width\": 464, \"height\": 448}, {\"url\": \"assets/figures/arxiv/2605.14421v1/fig-002.webp\", \"caption\": \"Figure 3: ASR matrix on the deterministic harness, rendered from paper/data/asr_matrix_v1.csv. Green cells are defended (ASR = 0); red cells are attacker wins (ASR = 1). Cell text gives the value and verdict, the right strip reports blocked attack families per defence, and the blue outline highlights the all-zero MemLineage row. Same numbers as Table 1; the visual is what reviewers see first, the table is what reviewers cite.\", \"page\": 14, \"index\": 2, \"width\": 504, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2605.14421v1/fig-003.webp\", \"caption\": \"Table 7: Per-operation median latency on a single core, n = 200 samples per op (Merkle proofs at n = 100). Source: paper/data/perf_baseline_v1.csv; numbers are wall-clockdependent and will vary by hardware, so the CSV is regenerated rather than byte-equal CI-verified.\", \"page\": 17, \"index\": 3, \"width\": 356, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2605.14421v1/fig-004.webp\", \"caption\": \"Figure 6: τ × K ablation across all five attribution configurations (140 cells). Green = propagated = 1 (the External label survives to the chain tip; the gate refuses); red = propagated = 0 (the chain tip lost the External label per the no-strong-parent fallback in §3.5). Gold-outlined cells are the K-discriminating cells: rows where increasing K flips the outcome. Panels (a)–(c) are flat in K; panels (d) and (e) are the K-degrading schedules (d = 0.7) where the interaction the prose discusses becomes observable. Source: paper/data/r_k_ablation_v1.csv, byte-equal CI-verified.\", \"page\": 17, \"index\": 4, \"width\": 1040, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2605.14421v1/fig-005.webp\", \"caption\": \"Figure 1: MemLineage architecture: six modules around a single memory store M . The write path (top) commits an entry through M1 metadata, M2 per-principal Ed25519 signing, and M3 RFC-6962 Merkle logging; the read + gate path (bottom) retrieves through M5, attaches trust labels via M4 lineage propagation, and refuses sensitive tool calls at M6 whose justifying context carries a DerivedUntrusted ancestor. Adversary-reachable inputs (red) are the only path through which untrusted content reaches M ; the rest of the pipeline is host-trusted (§2).\", \"page\": 5, \"index\": 5, \"width\": 492, \"height\": 438}]"
motivation: 不可信内容可被写入LLM代理的持久状态，并在后续会话中作为指令重新注入，导致敏感动作被恶意触发，现有过滤方法不足。
method: MemLineage采用六模块设计，基于RFC-6962 Merkle日志和Ed25519签名，通过加权派生DAG记录内存影响链，并利用最大强边传播规则确保不可信路径持续性，结合敏感动作门控拒绝源自外部祖先的调度。
result: 在确定性机制隔离框架和AgentDojo评估中，MemLineage在所有内存中毒工作负载下将攻击成功率降至零，且每次操作开销亚毫秒，而基线方法全部失败。
conclusion: MemLineage有效防止内存中毒攻击，同时保留良性记忆回忆，性能开销极低，为LLM代理内存安全提供了可验证的链式监管方案。
---

## 摘要
我们提出了MemLineage，一种针对LLM代理内存的防御机制，它为每个条目附加了密码学来源和LLM中介的衍生血缘。最近和当前的研究表明，不可信内容可以被写入持久代理状态，并在后续会话中作为指令重新进入；剩余的系统问题是如何在防止此类状态证明敏感操作合理性的同时，保留有用的内存召回。MemLineage将此视为一个监管链问题而非过滤问题。其设计围绕一个基于RFC-6962的Merkle日志，对每个主体的Ed25519签名条目，包含六个模块：加权派生DAG记录哪些检索到的条目影响了每个新内存，并且最大强边传播规则使得任何属性边保持在阈值以上的链都满足不可信路径持久性。敏感操作门随后拒绝那些主动理由源于外部祖先的调度，同时仍允许良性召回。我们在确定性机制隔离框架上，针对三种记忆中毒工作负载评估了三种防御单元；在该框架中，MemLineage是唯一使所有三列攻击成功率降至零的配置，而每次操作的亚毫秒开销使其远低于任何LLM调用的噪声底线。基于Codex的AgentDojo桥进一步将强模型行为与防御层行为分离：在故意易受攻击的工具输出配置下，无防御和仅签名基线在所有六个银行交易对上都失败，而所有MemLineage行都将严格AgentDojo攻击成功率降至零。核心确定性工件通过字节相等的CI验证；托管模型的AgentDojo和实时模型扫描被记录为可审计日志，而非字节固定的工件。

## Abstract
We introduce MemLineage, a defense for LLM agent memory that attaches both cryptographic provenance and LLM-mediated derivation lineage to every entry. Recent and concurrent work shows that untrusted content can be written into persistent agent state and re-enter later sessions as an instruction; the remaining systems question is how to preserve useful memory recall while preventing such state from justifying sensitive actions. MemLineage treats this as a chain-of-custody problem rather than a filtering problem. It is a six-module design around an RFC-6962 Merkle log over per-principal Ed25519-signed entries: a weighted derivation DAG records which retrieved entries influenced each new memory, and a max-of-strong-edges propagation rule makes Untrusted-Path Persistence hold for any chain whose attribution edges remain above threshold. The sensitive-action gate then refuses dispatches whose active justification descends from an external ancestor, while still allowing benign recall. We evaluate three defense cells against three memory-poisoning workloads on a deterministic mechanism-isolation harness; MemLineage is the only configuration in that harness that drives all three columns to zero ASR, while sub-millisecond per-operation overhead keeps it well below the noise floor of any LLM call. A Codex-backed AgentDojo bridge further separates strong-model behavior from defense-layer behavior: under an intentionally vulnerable tool-output profile, no-defense and signature-only baselines fail on all six banking pairs, while all MemLineage rows reduce strict AgentDojo ASR to zero. The core deterministic artifacts are byte-equal CI-verified; hosted-model AgentDojo and live-model sweeps are recorded as auditable logs rather than byte-pinned artifacts.